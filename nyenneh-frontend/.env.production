# Point this at the deployed Django API before running `npm run build`.
VITE_API_BASE_URL=https://api.example.com/api

# Set this to the registry's real WhatsApp number before running `npm run build`.
VITE_WHATSAPP_NUMBER=231770000000
